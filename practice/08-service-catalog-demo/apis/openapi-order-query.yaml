openapi: 3.0.3
info:
  title: Order Query API
  version: 1.0.0
paths:
  /orders/{orderId}:
    get:
      summary: Get order summary
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
