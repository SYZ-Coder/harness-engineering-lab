# AI Review Checklist

- Is the function name clear enough for future agents and developers?
- Are invalid inputs rejected explicitly instead of being handled implicitly?
- Does the implementation match what `README.md` claims the demo is validating?
- Are there hidden edge cases around discount, shipping, or rounding?
- Is the code small enough that an agent can reason about it reliably?

